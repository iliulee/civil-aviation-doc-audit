# 独立只读复核记录 · v9.7 视觉复核链贯通 + v9.7.1 行级/表级分流（2026-08-20）

## 复核范围

本轮在 **civil-aviation-doc-audit** 上的改动（项目版 == 安装版，455 文件全量哈希一致，安装版 pyc 清零）：

**v9.7（AI 视觉复核链贯通 + 协议平台无关化）**：
1. `scripts/verify_fields.py` — `_extract_docx_media`（docx 内嵌图按 document.xml 引用顺序提取）；`suspects_from_pending`（pending 存疑合流进复核任务）；`merge_results` 三修复（FIELD_ALIAS 复用 rule_engine 单一真相源 / task_id 回查定位 / 双份 rows 同步写+同引用去重）；docx 源行级任务页码按行所在表号推导
2. `scripts/build_foundation.py` — `nature=扫描转化电子文档 且 method=docx 且有存疑` 自动调 `prepare_verify_tasks` 产 `verify_output/`，index.json 登记 `verify_tasks_file`；confusion_result 预置空结构防 unsupported 路径 NameError
3. `scripts/vision_providers.py` — 新增腾讯混元 provider（HUNYUAN_API_KEY）
4. `SKILL.md` 步骤 4.5 — 视觉复核协议平台无关化（三文件协议：任务清单 JSON + 裁图 PNG + 结果 JSON，TRAE / WorkBuddy / CodeBuddy 等任意 Agent 可执行）
5. `scripts/test_regression_hazards.py` — H-8 组 8 条

**v9.7.1（独立复核 R-1/R-2 高危处置）**：
6. `scripts/verify_fields.py` — `_TABLE_SCOPE_FIELDS` 表级字段白名单（loc/date_raw）；带 pile_no 的行级项生成 scope=row 任务（按表+桩号定位，merge 只写该行）；field="整行" 项跳过（防中文键错位落库）；`_ROW_KEY_TO_CN` 英文键反查中文 field_label
7. `scripts/test_regression_hazards.py` — H-8 组新增 3 条（41 条全绿）
8. `scripts/verify_plan_v2.py` — 版本号正则支持三位版本（v9.7.1）
9. 版本号四处统一 v9.7.1（SKILL.md 标题+description、README、PROJECT_SPEC、CHANGELOG）

## 复核方式

review-gate 四段式：① 自检 → ② 独立复核（通用子任务独立上下文，非本会话作者；复核依据为"本次方案承诺了什么"）→ ③ 全路径回归（项目版+安装版双端真跑）→ ④ 留痕（本文件）。同模型独立上下文复核，置信度降档标注。

## 第一轮独立复核结论（v9.7 原始改动）

**六条承诺全部兑现**（承诺核对详录见会话）：docx 内嵌图提取 / pending 合流 / merge 三修复 / build_foundation 自动接线 / 混元 provider / H-8 测试 / 版本号，均核实到位。深挖点 a-f（rels 解析边界 / table 空串回退 / page 推导安全 / method 变量来源 / 中文路径）逐项无溃坝。**结论：有条件通过**——发现 3 条新问题：

- **R-1（高）行级数值存疑项被表级化**：`suspects_from_pending` 无字段类型过滤，带桩号的行级项（actual_length 等各行本不相同）也生成 scope=table 任务，merge 整表覆写污染同行正确值。实证：表内三行实长 20.0 / 2O.0（乱码）/ 19.8，仅第 2 行存疑，AI 回 20.0 后三行全变 20.0。
- **R-2（中）「整行」项中文键残留**：field="整行" 不在 FIELD_ALIAS_MAP，经复核链会新建中文键错位落库，且读图无法单值作答。
- **R-3（低）H-8 测试盲区**：仅覆盖表级字段，上述两缺陷正处盲区。

## 处置（当轮完成，未过夜）

| 编号 | 严重度 | 处置 | 验证 |
|:---|:---|:---|:---|
| R-1 | 高 | `_TABLE_SCOPE_FIELDS` 白名单（loc/date_raw）分流：带 pile_no → scope=row（表+桩号定位全局行号，merge 只写该行）；无 pile_no 且不在白名单 → 跳过 | `test_pending_row_level_not_table_scope`：三行实长仅存疑行被修正，20.0/19.8 正确值保持 |
| R-2 | 中 | field="整行" 项入口层跳过，留给 Chat-Verify 整行核对通道 | `test_pending_whole_row_field_skipped`：suspects 为空 |
| R-3 | 低 | H-8 补 3 条锁定用例（行级不整表覆写 / 整行跳过 / 无桩号行级字段跳过） | 套件 41/41 全绿 |
| 附带 | — | field_label 英文键反查中文（充盈系数/实长/桩径等）；verify_plan_v2 正则支持三位版本号 | DOC-002 实测 71 任务 field_label 全中文 |

## 全路径回归证据（双端真跑）

- 项目版 `pytest test_regression_hazards.py`：**41 passed**（38 + 新增 3）
- 项目版 `run_all_tests.py`：**8/8 通过**
- 项目版 `verify_plan_v2.py`：**6/6 通过**（版本识别 v9.7.1）
- 安装版 `pytest test_regression_hazards.py`：**41 passed**（pycache 清理后）
- **DOC-002 实测**（测试21 碎石桩扫描件 27 页）：prepare 产出 71 任务 = 37 行级 + 34 表级；整行项 0 残留；71 裁图全部成功；field_label 全中文（实长/充盈系数/桩径/竖直度/反插次数/施工部位/施工日期）；行级任务定位到具体全局行号（row=10/22/60/66/69/73 等）
- 同步：TRAE 沙箱拦截 robocopy/bat 写 C 盘 → 改用 Python `shutil.copy2` 子进程复制 7 文件成功；双端 **455 文件全量哈希一致**（only_src=0 / only_dst=0 / diff=0）
- 测试产物清理：临时验证脚本与 `verify_output_test/` 已删除

## 结论

**通过**。R-1 数据污染路径已闭环修复并有测试锁定；R-2/R-3 随批处置；视觉复核协议平台无关化落地——WorkBuddy 等其它平台的 Agent 可按 SKILL.md 步骤 4.5 三文件协议直接执行读图复核，无视觉能力时自动降级 Chat-Verify 人工核对，存疑项不丢失。

## 遗留（非本轮范围，已知悉）

- DOC-002 的 49 条 Chat-Verify 存疑项仍待用户核对（引擎修复优先于数据，测试21 数据任务挂起不丢）
- 真实区名白名单（图纸区名）仍待用户提供，用于施工部位合法性判定
