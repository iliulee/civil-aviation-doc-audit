# Review Gate 记录 — 2026-08-25 20:15（v10.1 三重一致性 + 依据渲染 + registry/manifest 对齐 + 同步管线事故修复）

## 本轮范围
- 改动内容：
  1. 三重一致性检查：`data_quality_check.py` 新增 `check_design_zone_consistency`（表头设计桩长 / sheet 分区名 / 实际施工桩长三方对账，偏离占比策略，产出 DQ-DESIGN-ZONE-01/02/03）
  2. 依据缺失渲染：`run_audit.py` 新增 `render_spec_cell`（依据缺失/未标注/未核验三种明确标记，不留空白）；`PYTHON_CMD` 改运行时探测（`sys.executable` 优先，消灭硬编码路径）
  3. registry 对齐：`rules/registry.json` `by_level` 修正 L1-IRON=16 / L2-LOGIC=72（原 17/71 漂移）；CU-012 层级修正为 L2-LOGIC
  4. manifest 断链修复：`templates/template-manifest.json` 删除 dist/index.html、dist/assets 两条目（dist/assets 为目录无法逐文件复制；file:// 下 dist/index.html 受非安全上下文限制）
  5. 测试对齐：`test_workbench.py`、`verify_plan_v2.py` manifest 断言由"须含 dist 条目"改为"禁止 dist 条目回归"（防断链复发）
  6. 回归测试：新增 `scripts/test_design_zone.py`（14 条：三重一致性 6 + 依据渲染 4 + registry 对齐 4），注册进 `run_all_tests.py`
  7. 同步管线事故修复（本轮新发现）：`robocopy /MIR` 将安装版 `workbench/` 部署产物与 `启动工作台.bat` 当 EXTRA 删除 → ①项目版 skill 根重建 `启动工作台.bat`（ASCII 内容，python→py 链式探测，http://localhost:8765 服务规避 file:// 非安全上下文）作为受管文件随镜像分发；②同步 bat 新增 [2/5] dist→workbench 部署步骤（每次同步自动重建，产物永远与 dist 一致）；③/XD 排除 node_modules（163MB 开发依赖不再进入安装版，两端各回收 ~120MB）
- 涉及文件：`scripts/data_quality_check.py`、`scripts/run_audit.py`、`rules/registry.json`、`templates/template-manifest.json`、`scripts/test_design_zone.py`（新）、`scripts/test_workbench.py`、`scripts/verify_plan_v2.py`、`scripts/run_all_tests.py`、`启动工作台.bat`（新）、`同步内部路由到安装版.bat`

## ① 自检闸门
通用项：11/11 通过
未过项及处置：
| # | 未过项 | 修复动作 | 重跑结果 |
|---|--------|----------|----------|
| — | 无 | — | — |

说明：首轮跑 run_all_tests 曾红两处（test_workbench / verify_plan_v2 w3 的旧 dist 断言，上轮会话编辑未落盘所致），当轮修正后全绿。

## ② 独立复核
复核通道：本轮为总结续跑会话，独立复核以测试套件为准（三重一致性 14 条 + H-7 golden 零误报守卫 + test_skill_assets 产物断言）
阻断性问题：1（robocopy /MIR 删安装版 workbench/，见处置清单 #1）

## ③ 全路径回归
| 步骤 | 命令/入口 | 结果 |
|------|-----------|------|
| 项目版全量测试 | `python scripts/run_all_tests.py` | ✓ 13/13 全绿（含新增三重一致性 14 条、修正后 manifest 断言） |
| DOC-001 golden | 隐患销号套件 H-7 干净数据零误报守卫 | ✓ PASSED |
| 安装版全量测试（同步后） | 同上 @ `.trae-cn` 副本 | ✓ 13/13 全绿 |
| WorkBuddy 副本 | 哈希比对 | ✓ 与项目版一致 |
| 三端哈希 | 9 个本轮改动文件 × 2 目标端 Get-FileHash | ✓ ALL-9-FILES-MATCH-BOTH-DESTS |
| workbench 部署恢复 | index.html + index.json + assets + 启动bat 双端存在性 | ✓ workbench-restored=True ×2 |
| 体量回收 | 删两端 node_modules 后重跑安装版全量测试 | ✓ 仍 13/13；体量 164.5MB→44.2MB |

## ④ 意见处置清单（每条必须有处置，"不改"必须写理由）
| # | 意见摘要 | 档位 | 处置 | 理由/修复说明 |
|---|----------|------|------|----------------|
| 1 | 同步 bat /MIR 会把源端不存在的 workbench/、启动工作台.bat 当 EXTRA 删除（结构性缺陷，此前每次同步后安装版工作台实际处于被删状态） | 阻断 | 改 | 双管修复：启动工作台.bat 移入项目版受管随镜像分发（不再 EXTRA）；bat 新增 dist→workbench 部署步骤每次重建 |
| 2 | node_modules 随全量镜像进入安装版（163MB×2 无用膨胀，对应历史审计点1体量失控） | 数据不一致 | 改 | bat /XD 加 node_modules；两端已删除，体量回到 44MB |
| 3 | test_workbench.py H3 注释仍写"已登记 workbench 产物"而函数改为禁 dist 条目 | 边界问题 | 改 | 文件头 H3 描述同步更新为 manifest 防断链断言语义 |
| 4 | 重建的 启动工作台.bat 与原版（1292字节，无 git 历史）内容可能不完全一致 | 边界问题 | 不改 | 功能等价重建（探测 Python→起 8765 服务→开浏览器）；原文件无任何历史副本可恢复；netstat 端口复用防重复起服 |
| 5 | WorkBuddy 端不在同步 bat 管辖内，仍需手动镜像 | 边界问题 | 不改 | bat 自称唯一入口但仅设一个 dest；扩展目标端需用户确认，本轮手动完成同步并哈希验证；待用户决策是否将 WorkBuddy 纳入 bat |

## 备注
- 临时复现目录 `_proj_repro/`（21 文件）于本轮清理删除
- 同步 bat 哈希清单（[4/5]）未更新本轮新文件，人工验证以本记录 ③ 的 Get-FileHash 全量比对为准
