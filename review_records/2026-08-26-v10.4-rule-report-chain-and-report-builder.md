# Review Gate 记录 — 2026-08-26（v10.4 规则→审核→报告全链路贯通 + 报告三层结构）

## 本轮范围

A 组（规则链路修复）+ B 组（报告结构重构），共 2 个专项、10 项任务全部完成：

- **A 组（规则链路）**：
  1. `scripts/test_rule_to_report_chain.py`（新，10 条）：LG-110 触发词覆盖生产 doc_type / S-04 审核期重算 / 台账刷新与 corrected 数据一致 / review_audit 接线 / 规则执行统计 / 0 匹配 ⚠ 标记 / registry 计数与 SKILL.md 对齐 / 引擎不重复执行 LG-110 / CFG 桩通用规则覆盖
  2. `scripts/scan_rule_coverage.py`（新，诊断工具）：扫生产数据底座真实 doc_type（9 种）与规则触发词对账；输出零交集规则（3 条真高危）、通配规则归类（13 条 '*'）、生产覆盖缺口（6 种）
  3. `scripts/extract_certificates.py`：新增 `collect_certificate_findings`（审核期基于 corrected 数据重算 S-04）
  4. `scripts/review_audit.py`：步骤 5.6 接线 + 审核期原子刷新台账 + summary 注入 `rule_execution_stats`
  5. `scripts/rule_engine.py`：新增 `build_execution_stats`（每条规则 matched_docs/hits）
  6. `rules/L2-logic/LG-110.json`：触发词补「材料、构配件进场检验记录」；name 笔误修正（材料进→材料进场）
- **B 组（报告结构）**：
  1. `scripts/report_builder.py`（新）：build_model（纯建模）/render_html（纯渲染）分离，run_audit 改薄包装 + re-export 保持旧引用
  2. 三层报告结构：结论层（概要+结论）/ 问题层（发现清单含「整改建议」列）/ 行动层（规则执行统计 + 整改建议）
  3. 页脚版本号单一真相源：`_skill_version()` 读 SKILL.md frontmatter `version:`，不硬编码
  4. `scripts/verify_report.py`：新增 `check_certificates_alignment` 合格证台账对账闸门
  5. 文档：SKILL.md v10.4（+version 元数据+L1 计数 17→16 修正）/ README v10.4（+4 新文件登记+规则数 93→94）/ PROJECT_SPEC v10.4 / CHANGELOG v10.4 条目
  6. `scripts/run_all_tests.py`：注册「规则报告链」+「报告生成器」两套件

## ① 自检闸门

通用项：11/11 通过。过程中发现并当轮修复 3 处：

| # | 未过项 | 修复动作 | 重跑结果 |
|---|--------|----------|----------|
| 1 | D4 测试正则不兼容 frontmatter 带引号（`version: "10.4"` 匹配失败） | 正则补 `"?` 引号兼容（复核后再补 `^` 锚定 + re.M） | ✓ |
| 2 | verify_plan_v2 版本识别硬编码行号 `split("\n")[5]`（frontmatter 加字段即错位） | 改 H1 标题正则全文搜索 | 7/7 绿 |
| 3 | SKILL.md L1 计数宣称 17 条，实际目录 16 个文件 | 修正为 16（与 registry by_level 一致） | ✓ |

## ② 独立复核

复核通道：通用子代理只读复核（9 项检查：接口兼容/测试有效性/恒真断言排查/glob 模式/对账语义/正则命中/测试注册/registry 与文件对账/双端文件清单 diff）

结论：**可交付，零阻断**。4 条建议级意见当轮处置 3 条：

| # | 意见 | 档位 | 处置 |
|---|------|------|------|
| 1 | LG-110 name 漏「场」字（材料进检验记录），会显示在报告统计表 | 建议 | **改**：4 处同步修正（LG-110.json / registry.json / extract_certificates.py / CHANGELOG.md） |
| 2 | D6 测试死构造（tmp_path 文件未被消费）+ json 死 import | 建议 | **改**：删除死构造与死 import，改为直接传参 |
| 3 | D4 测试正则无行首锚定，弱于实现侧 | 边界 | **改**：补 `^` + re.M，与 `_skill_version` 同款 |
| 4 | scan_rule_coverage 通配混合声明（`["*","碎石桩施工记录"]`）会漏统计 | 边界 | 不改：当前规则库 17 条通配规则全部纯 `["*"]`，无实际影响；留后续迭代 |

## ③ 全路径回归

| 步骤 | 入口 | 结果 |
|------|------|------|
| 双套件单跑 | pytest test_report_builder + test_rule_to_report_chain | 16/16 绿 |
| 方案验收 | verify_plan_v2.py | 7/7 绿（含版本号统一四文档对齐） |
| 全量回归（项目版） | run_all_tests.py | **17/17 全绿**（含新增 2 套件） |
| 规则覆盖扫描实测 | scan_rule_coverage.py | 9 种生产 doc_type / 3 条零交集 / 6 种缺口 / CFG 桩 7 份文档被通用规则覆盖 |
| 同步后复核 | review_skill（同步 bat 内置） | 64/64 绿 |
| 双端哈希 | 17+1 改动文件分批验证 | 全部 MATCH（分批规避 PowerShell 长命令折行污染） |
| 双端文件清单 | scripts/ .py 双向 diff | 各 61 个，无单端独有文件 |

## ④ 停电恢复检查（本轮特殊事项）

用户报告中途停电。恢复后三步完整性检查：
1. 8 个改动 Python 文件 py_compile：全部语法完整
2. 9 处文档关键标记（version 10.4 / 94 条 / v10.4 条目等）：全部在位
3. run_all_tests 重跑：17/17 全绿

停电期间发现 1 个真实问题：`scan_rule_coverage.py` 此前宣称已创建但实际未落盘（疑似上轮会话写入未完成），本轮补写并实测验证。同时发现 PowerShell 哈希验证长命令存在折行污染（`_x000D__x000A_` 注入文件名）导致假阳性 MATCH，已改用分批短命令复验。

## ⑤ 遗留事项（不阻塞交付）

- scan_rule_coverage 通配混合声明防御（复核意见 #4，当前无实际影响）
- verify_report 单声称对账（仅第一处声称数受检，多处声称漂移不检出——设计取舍）
- 生产覆盖缺口 6 种 doc_type（设计变更文件 30 份/其他资料 16 份/施工日志 15 份等无 SINGLE_DOC 规则），属规则库待补事项，非本轮代码问题

## 结论

v10.4 A/B 两组专项全部完成，17/17 全量测试绿，双端同步一致，独立复核零阻断，可交付。
