# Review Gate 记录 — 2026-08-26（v10.5 无规则覆盖运行时闸门）

## 本轮范围

审核运行时"无规则覆盖"自动提醒专项，起因：用户发现规则引擎对规则库从未声明的 doc_type 静默跳过（不报错、不告警），AI 与用户均不知情"哪些类型从来没有规则管过"。

- **运行时侦测**：`scripts/rule_engine.py` 新增 `build_unguarded_doc_types`——按 SINGLE_DOC/CROSS_DOC（trigger_when.doc_type 名单，'*' 通配生效）+ CROSS_UNIT（doc_type_a/b 双侧）三类 scope 综合判定，任一命中即视为有覆盖；仅统计 `doc_role == 'audited'` 受审文档（缺省 None 视为受审），reference 角色不进提醒
- **审核接线**：`scripts/review_audit.py` summary 注入 `unguarded_doc_types`
- **报告渲染**：`scripts/report_builder.py` 行动层新增「八、无规则覆盖提醒」节（非空才出现，警示框+明细表）；取数兼容 `rule_engine_summary` 子 dict 与顶层结构
- **扫描工具校准**：`scripts/scan_rule_coverage.py` 复用同一判定函数（工具与运行时口径一致），仅统计受审文档，修复"设计变更文件 30 份伪缺口"误报
- **零交集规则修复**：IR-010 / IR-011 / LG-905 触发词补「场道施工记录」——三条规则因触发词与生产 doc_type 失配而静默失效，修复后零交集 3 → 0
- **文档**：SKILL.md 升版 v10.5（+AI 强制协议：生成报告前必检查 unguarded_doc_types，非空须向用户显式说明）/ CHANGELOG v10.5 条目
- **测试**：新增 `scripts/test_unguarded_doc_types.py`（8 条）注册进 `run_all_tests.py`

## ① 自检闸门

| 检查项 | 结果 |
|--------|------|
| 测试先红后绿（TDD） | ✓（侦测函数未接入前 G1/G2 必红） |
| 测试有效性（非恒真） | ✓ 8 条独立断言，正反用例齐全（点名/不误报/空列表） |
| run_all_tests 注册 | ✓ 注册为「无规则覆盖闸门」套件 |
| 接口兼容（外部签名不变） | ✓ build_unguarded_doc_types 为纯新增函数，无既有签名改动 |
| 双端同步 + 哈希 | ✓ 11 个本轮改动文件 SHA256 全一致 |
| 全量回归 | ✓ run_all_tests 18/18 全绿 |

过程中发现并当轮修复 2 处：

| # | 未过项 | 修复动作 | 重跑结果 |
|---|--------|----------|----------|
| 1 | 直接 `python` 跑 test_unguarded_doc_types.py 只定义函数不执行（退出码 0 假绿） | 确认必须走 pytest；注册进 run_all_tests.py 由统一入口调度 | ✓ 8 passed |
| 2 | 新增「八、无规则覆盖提醒」与既有「八、基于低置信识别需人工重点核实」章节编号冲突 | 既有章节顺延为「九、」；拼接顺序确认 七→八→九；测试无旧编号锁定 | ✓ 24 passed |

## ② 独立复核（只读检查）

| # | 检查点 | 结论 |
|---|--------|------|
| 1 | review_audit.py:1156 侦测接线位置 | ✓ summary 字段注入正确，matcher 复用 |
| 2 | report_builder.py:857 `{unguarded_section}` 拼接 | ✓ 位于 rule_stats 之后、ocr_review 之前 |
| 3 | 章节编号唯一性 | ✓ 修复后 一~九 无重复 |
| 4 | 渲染互斥性 | ✓ 全部有覆盖时 unguarded_section 为空串，整节不出现（不硬造提醒） |
| 5 | 零交集修复有效性 | ✓ scan_rule_coverage 复扫：零交集 0 条；覆盖缺口仅剩「其他资料」16 份（兜底类型，报告自动提醒，可接受） |
| 6 | 施工日志覆盖确认 | ✓ 本就有 LG-901/902/906/106/108/301/606/607、IR-017、CU-009/011 等多条规则，此前"缺口"系 reference 角色混入+统计口径错误的伪缺口 |

结论：**可交付，零阻断**。

## ③ 全路径回归

- `run_all_tests.py`：18/18 全绿（含新注册「无规则覆盖闸门」8 条）
- `scan_rule_coverage.py`：零交集规则 0 条；生产覆盖缺口 1 种（其他资料 16 份，已知可接受）
- `verify_skill_structure`：4/4 通过；`review_skill`：64/64 通过（同步脚本内置）

## ④ 留痕闭环

- 双端同步：`同步内部路由到安装版.bat` 全量镜像 + workbench 部署 + pycache 清理
- 哈希验证：本轮 11 个改动文件（rule_engine / review_audit / report_builder / run_all_tests / scan_rule_coverage / test_unguarded_doc_types / IR-010 / IR-011 / LG-905 / CHANGELOG / SKILL.md）双端 SHA256 全一致
- 文档：SKILL.md v10.5、CHANGELOG v10.5、本记录

## 遗留（非阻断）

- 「其他资料」16 份无规则覆盖：属兜底 doc_type，暂无对应规范条款可编规则；报告第八节会自动提醒，用户知情接受即可。若后续要收敛，需先在内容感知分类阶段把"其他资料"细分到具体类型。
