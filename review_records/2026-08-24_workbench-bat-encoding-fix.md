# Review Gate 记录 — 2026-08-24

## 本轮范围
- 改动内容：修复「资料员工作台」启动 bat 的 cmd 乱码报错（"'...'不是内部或外部命令"）
- 涉及文件：`资料员工作台\启动工作台.bat`
- 待办清单版本/来源：用户报障「启动工作台是这个情况」，触发 review-gate

## ① 自检闸门
通用项：10/10 通过
未过项及处置：无

## ② 独立复核
复核通道：降级 · 通用子任务 · 同模型 · 置信度降档
意见总数：2（边界问题 b：启动顺序 / 错误提示误导性 c）
阻断性问题：0

## ③ 全路径回归
| 步骤 | 命令/入口 | 结果 |
|------|-----------|------|
| 编码校验 | `[System.IO.File]::ReadAllBytes` 检查非 ASCII 字节 | PASS，非ASCII字节=0（纯ASCII） |
| 实跑 bat | `& 启动工作台.bat`（还原真实 cmd 环境） | 干净启动：echo 正常打出，无任何"不是内部或外部命令" |
| 服务响应 | `Invoke-WebRequest http://localhost:8000/index.html` | HTTP 200，页面可访问 |

## ④ 意见处置清单（每条必须有处置，"不改"必须写理由）
| # | 意见摘要 | 档位 | 处置 | 理由/修复说明 |
|---|----------|------|------|----------------|
| 1 | `start` 在服务就绪前开浏览器，可能首帧失败 | 低 | 不改 | 服务约 1 秒内就绪，首帧失败自愈（刷新即好），为 batch 常见写法；改先起服务再开浏览器需额外后台处理，反致脚本复杂 |
| 2 | Python 缺失提示对"端口被占用"同样误触发、措辞有误导 | 很低 | 不改 | 端口占用鲜见于本单机场景；措施保持简洁，不为低频分支堆文案 |

## 结论
- [x] 通过交付

## 沉淀（可选）
- 漏点1：中文内容 + Write 工具 UTF-8 落盘 → cmd GBK 代码页必乱。**建议追加 self-check-checklist 项目扩展位条目：任何交付的 `.bat`/`.cmd` 必须纯 ASCII，或显式以 GBK 落盘。**