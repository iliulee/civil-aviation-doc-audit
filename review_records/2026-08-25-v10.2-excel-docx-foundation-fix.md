# Review Gate 记录 — 2026-08-25 21:00（v10.2 Excel/docx 建底座链路修复）

## 本轮范围
- 改动内容（纯代码修复，不新增接口）：
  1. `build_foundation.py` `parse_excel_workbook_rows`：
     - P1：数据行过滤 `break`（整表截断）→ `continue`（单行跳）+ 无数字落款行才 break
     - P5：每行补 `row_index`（sheet 内物理行号）行级定位键
     - P4：可识别列追加英文标准槽位键（pile_no/actual_length…，桩列强别名覆盖序号弱别名）
     - P7：表头扫描窗口 20 → `_EXCEL_HEADER_SCAN=100` + 未命中 stderr 告警
     - P2(P2 部分)：子表头为纯单位（m/mm/%/时分…）不拼「·单位」
     - P6：`_excel_maybe_date_text` 数字日期序列号/date 对象 → `YYYY-MM-DD`
  2. `build_foundation.py` `parse_docx_table_sheets`：结构化行补 `row_index`（表内 1-based）
  3. `data_quality_check.py` `check_column_shift`（P10 新发现）：`_is_num` 由 isinstance(int/float) 改为宽松 float 判定（底座行为字符串，原逻辑对全字符串数据百分百误报）；缺失值（None/空串）跳过；数学链改用 `_to_float` 防 str-str TypeError
  4. 回归测试：新增 `scripts/test_xlsx_docx_chain.py`（10 条：X1-X7 + P10 误报/捕获 + docx row_index），注册进 `run_all_tests.py`
- 涉及文件：`scripts/build_foundation.py`、`scripts/data_quality_check.py`、`scripts/test_xlsx_docx_chain.py`（新）、`scripts/run_all_tests.py`

## ① 自检闸门
通用项：11/11 通过
未过项及处置：
| # | 未过项 | 修复动作 | 重跑结果 |
|---|--------|----------|----------|
| 1 | 新测试 7 红（预期：修复未实现） | 实现后 8/8 绿，追加 P10 2 条后 10/10 绿 | ✓ |
| 2 | 复验发现 DQ 对 CFG 全量误报 3711 条（`_is_num` 只认数值类型） | P10 修复：字符串数值宽松判定 + 缺失跳过 | 误报降至 2 条（真实乱码题） |

## ② 独立复核
复核通道：以回归套件 + 真实文件复验为准（先红后绿留痕）
阻断性问题：1（P10 质检全量误报，见处置清单 #1）

## ③ 全路径回归
| 步骤 | 命令/入口 | 结果 |
|------|-----------|------|
| 先红 | `pytest test_xlsx_docx_chain.py`（修复前） | 7 failed / 1 passed（符合预期） |
| 修复后单套件 | 同上 | 10/10 全绿 |
| 真实 CFG xlsx 复验 | `parse_excel_workbook_rows(测试23 CFG桩)` | rows=3711 不变；row_index=10 首；pile_no='1142'（桩列强别名）；date_raw='2025.11.13'；单位列名干净 |
| 真实 CFG DQ 复验 | DataQualityChecker.run_all | 误报 3711→2 条，2 条为真题（第1592/2282行「筑业软件 48545..」水印乱码入数据列） |
| 施工日志 xlsx | parse | 0 行（设计内，无桩号列）；告警不再刷屏 |
| 真实 docx 复验 | `parse_docx_table_sheets(测试21 27页)` | 146 行 + 49 存疑不变；row_index=1,2 ✓ |
| 复杂表头样例 | 单位列+合并单元格 | rows=2；「实长」（无·m）；'实长·实际量' 保留；pile_no=Z101 ✓ |
| 全量回归（项目版） | `run_all_tests.py` | 14/14 全绿（含 H-7 golden 无倒退） |
| 全量回归（安装版 .trae-cn） | 同上 | 14/14 全绿 |
| 三端哈希 | 4 个改动文件 × 2 目标端 | all-hash-match=True |
| workbench 部署 | dist→workbench 同步 | ✓ 两端恢复 |

## ④ 意见处置清单
| # | 意见摘要 | 档位 | 处置 | 理由/修复说明 |
|---|----------|------|------|----------------|
| 1 | DQ `check_column_shift._is_num` 只认 int/float → 底座字符串数据（Excel/docx/OCR）百分百误报非数值 | 阻断（新发现 P10） | 改 | 宽松 float 判定 + bool 排除；缺失（None/空串）跳过；数学链 `_to_float` 防 TypeError |
| 2 | 「序号」列被 OCR 别名抢注 pile_no，挤压「桩位编号」 | 数据不一致 | 改 | 桩列（含"桩"字）强别名覆盖，弱别名后写不覆盖 |
| 3 | 表头未命中告警在无表头 sheet（施工日志×30）刷屏 | 边界 | 改 | 告警条件收紧为含 桩/设计/灌入/充盈 特征 |
| 4 | Excel 行仍保留幽灵列（列18/列22 等）与「红色·数字化…」原始列 | 边界 | 不改 | 属原始表列，删除会引发列索引偏移；待后续列剪枝策略，不阻塞本轮 |
| 5 | DQ-SELF-PILE-01 遇空桩号提前 return（跳过重复号检查） | 边界 | 不改 | 既有行为；空桩号行本身即需人工核对，不扩大本轮范围 |

## 备注
- 修复后 Excel 链路从「丢数据不报警 + 质检静默」变为「不截断 + 行可定位 + 质检可抓到真实乱码」（CFG 第1592/2282行「筑业软件」水印即验证样例）
- 未修改 SKILL.md/PROJECT_SPEC.md（row_index 本就在 schema 契约中，本轮是补齐实现）